{
  "name": "@skolist/e2e",
  "version": "0.0.1",
  "private": true,
  "description": "End-to-end tests for Skolist (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed --workers=1",
    "test:ui": "playwright test --ui",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^25.2.3",
    "dotenv": "^16.5.0",
    "typescript": "^5.9.3"
  }
}
