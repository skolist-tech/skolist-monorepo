name: "Cloud Run Deploy (Stage) (Backend)"

on:
  push:
    branches:
      - stage
    paths:
      - "backend/**"

env:
  SERVICE_NAME: "backend-stage"

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SA_EMAIL }}

      - name: Deploy to Cloud Run
        id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          region: ${{ secrets.GCP_REGION }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          source: "./backend"
          flags: |
            --allow-unauthenticated
            --set-secrets=SUPABASE_URL=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_URL_STAGE:latest
            --set-secrets=SUPABASE_SERVICE_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_SERVICE_KEY_STAGE:latest
            --set-secrets=SUPABASE_ANON_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_ANON_KEY_STAGE:latest
            --set-secrets=GEMINI_API_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/GEMINI_API_KEY_STAGE:latest
            --set-secrets=OPENAI_API_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/OPENAI_API_KEY_STAGE:latest
            --set-secrets=TEST_USER_EMAIL=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/TEST_USER_EMAIL_STAGE:latest
            --set-secrets=TEST_USER_PASSWORD=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/TEST_USER_PASSWORD_STAGE:latest
            --set-env-vars=LOGGING_LEVEL=DEBUG
            --set-env-vars=DEPLOYMENT_ENV=STAGING

      - name: Output Deployment URL
        run: echo "Deployed to ${{ steps.deploy.outputs.url }}"
