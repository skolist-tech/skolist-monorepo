name: "Cloud Run Deploy (Main) (Backend)"

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"
  workflow_dispatch:

env:
  SERVICE_NAME: "backend-main"

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SA_EMAIL }}

      - name: Deploy to Cloud Run
        id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          region: ${{ secrets.GCP_REGION }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          source: "./backend"
          flags: |
            --allow-unauthenticated
            --set-secrets=SUPABASE_URL=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_URL:latest
            --set-secrets=SUPABASE_SERVICE_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_SERVICE_KEY:latest
            --set-secrets=SUPABASE_ANON_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_ANON_KEY:latest
            --set-secrets=GEMINI_API_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/GEMINI_API_KEY:latest
            --set-secrets=OPENAI_API_KEY=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/OPENAI_API_KEY:latest
            --set-secrets=SMS_HOOK_SECRET=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/SUPABASE_SMS_HOOK_SECRET:latest
            --set-env-vars=FIREBASE_CREDENTIALS=projects/${{ secrets.GCP_PROJECT_NUMBER }}/secrets/FIREBASE_CREDENTIALS:latest
            --set-env-vars=LOGGING_LEVEL=INFO
            --set-env-vars=DEPLOYMENT_ENV=PRODUCTION
            --set-env-vars=PING=TRUE
            --set-env-vars=LOG_IMAGES=FALSE

      - name: Output Deployment URL
        run: echo "Deployed to ${{ steps.deploy.outputs.url }}"
