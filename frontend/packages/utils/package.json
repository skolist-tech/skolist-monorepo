{
  "name": "@skolist/utils",
  "version": "0.0.1",
  "private": true,
  "description": "Shared utilities for Skolist frontend apps",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "build": "tsc --emitDeclarationOnly --outDir dist"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.6.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
