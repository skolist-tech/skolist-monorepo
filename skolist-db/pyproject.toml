[project]
name = "skolist-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv==1.2.1",
    "supabase==2.27.0",
]

[tool.uv]
package = false
