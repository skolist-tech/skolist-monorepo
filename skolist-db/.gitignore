# Supabase internal folders
supabase/.temp

# Local environment variables (IMPORTANT: do not leak these)
.env
.env.local

# Virtual Env
venv

# cache files
__pycache__/

# Local-only python seed scripts (auto-discovered by seed.py; never commit)
# Name them: python_seeds/_local_seed_<anything>.py
python_seeds/_local_seed_*.py

# Large local full-paper modules (JEE/NEET mocks for demos; never commit)
python_seeds/data/_local_papers/